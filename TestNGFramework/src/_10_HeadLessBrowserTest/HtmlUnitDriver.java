package _10_HeadLessBrowserTest;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.testng.annotations.Test;
//import org.openqa.selenium.htmlunit.HtmlUnitDriver;

public class HtmlUnitDriver {
	
	@Test
	public void htmlDriver() {
		
		/*WebDriver htmlDriver= new HtmlUnitDriver();
		htmlDriver.get("https://www.google.com/");
		
		htmlDriver.findElement(By.name("q")).sendKeys("Selenium");
		
		System.out.println(htmlDriver.getTitle());
		*/
	}
	
}
